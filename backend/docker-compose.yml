version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: finzapp-postgres
    environment:
      POSTGRES_USER: finzapp
      POSTGRES_PASSWORD: finzapp
      POSTGRES_DB: finzapp_db
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - finzapp-network
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U finzapp']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: finzapp-redis
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    networks:
      - finzapp-network
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: finzapp-backend
    environment:
      DATABASE_URL: postgresql://finzapp:finzapp@postgres:5432/finzapp_db?schema=public
      REDIS_URL: redis://redis:6379
      JWT_SECRET: ${JWT_SECRET:-default-secret-change-in-production}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-default-refresh-secret-change-in-production}
      JWT_EXPIRES_IN: 15m
      JWT_REFRESH_EXPIRES_IN: 30d
      NLP_SERVICE_URL: http://nlp-service:8000
      INGEST_API_KEY: ${INGEST_API_KEY:-default-ingest-key}
      PORT: 3000
      NODE_ENV: development
      CORS_ORIGIN: http://localhost:3000
    ports:
      - '3000:3000'
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - finzapp-network
    volumes:
      - ./src:/app/src
      - ./logs:/app/logs
    command: npm run start:dev

  nlp-service:
    image: finzapp-nlp:latest
    container_name: finzapp-nlp
    environment:
      PORT: 8000
    ports:
      - '8000:8000'
    networks:
      - finzapp-network
    # Build this image from the nlp-service directory

volumes:
  postgres_data:
  redis_data:

networks:
  finzapp-network:
    driver: bridge
